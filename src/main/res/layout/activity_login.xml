<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/bg"
    tools:context="com.example.hasee.collage.activity.LoginActivity">

   <view.TitleView
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       app:tv_title="HITNEWS"
       app:tv_tv_right_visibility="gone"
       android:id="@+id/login_title"

       >

   </view.TitleView>

    <view.MEditText
        android:id="@+id/login_account"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:me_del_enable="true"
        app:me_eye_enable="false"
        app:me_hint=""
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        app:input_type="normal"
        app:me_lable_src="@drawable/account_focus"
        />

    <view.MEditText
        android:id="@+id/login_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:me_del_enable="true"
        app:me_eye_enable="true"
        app:me_hint=""
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        app:input_type="password"
        app:me_lable_src="@drawable/pwd_focused"

        />

    <Button
        android:layout_marginTop="30dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"
        android:id="@+id/login_sign_in"
        android:textColor="@color/white"
        android:text="登录"
        android:background="@color/bt_pressed"
        />
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/sign_up"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="快速注册"
            android:textSize="16sp"
            android:textColor="@color/title_text_color"
            android:padding="10dp"
            android:layout_alignParentBottom="true"
            />
        <TextView
            android:id="@+id/login_resrt_pwd"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="重置密码"
            android:visibility="gone"
            android:layout_alignParentRight="true"
            android:layout_alignParentBottom="true"
            android:padding="10dp"
            android:textColor="@color/title_text_color"
            android:textSize="16sp"
            />


    </RelativeLayout>
</LinearLayout>
